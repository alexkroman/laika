<!-- Start Information Source Module -->
<% remote_form_for(:information_source, 
                   :url => patient_data_instance_information_source_path(patient_data),
                   :update => 'informationSourceContainer', :method => http_method(information_source)) do |f| %>
<h5>Information Source</h5>
<table>
  <tr class="<%= cycle("darkzebra", "lightzebra")%>">
    <td class="lighttext">Time</td>
    <td><%= f.calendar_date_select :time %></td>
  </tr>
  <%= render :partial => '/person_names/edit', :locals => {:person_name => information_source.person_name}%>
  <tr class="<%= cycle("darkzebra", "lightzebra")%>">
    <td class="lighttext">Document ID</td>
    <td>
      <%= f.text_field :document_id %>
    </td>
  </tr>
  <tr class="<%= cycle("darkzebra", "lightzebra")%>">
    <td class="lighttext">Represented Organization Name</td>
    <td>
      <%= f.text_field :organization_name %>
    </td>
  </tr>
  <tr class="<%= cycle("darkzebra", "lightzebra")%>">
    <td></td>
    <td><%= f.submit "Save" %></td>
  </tr>
</table>
<% end %>